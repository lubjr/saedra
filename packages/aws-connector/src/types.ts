export type AwsCredentials = {
    accessKeyId: string
    secretAccessKey: string
    region: string
}

export type AwsResource = {
    id: string
    type: string
    name: string
    region: string
}